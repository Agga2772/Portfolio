/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: "https://agga-portfolio.vercel.app", // Change this to your actual domain when you have one
    generateRobotsTxt: true, // Generates a robots.txt file
    sitemapSize: 5000,
  };
  
